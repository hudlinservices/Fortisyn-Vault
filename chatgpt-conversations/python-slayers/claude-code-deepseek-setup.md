---
title: "Claude Code DeepSeek Setup"
date: "2026-06-15"
category: chatgpt-export
source: "Full ChatGPT export"
messages: 104
entity: python-slayers
---

# Claude Code DeepSeek Setup

**Messages:** 104 | **Entity:** python-slayers

### 1
Can I install claude code in the VScode and then tie it to deepseak account I already have?

### 2
I stated I want to install it within the Ubuntu instance in VSCode

### 3
Where did this come from?

### 4
I have a new laptop so I have to make much of these connections all over again

### 5
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion


*Full conversation in `/home/royhudlin/chatgpt-extract/`.*
